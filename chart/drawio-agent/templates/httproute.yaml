apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "drawio-agent.fullname" . }}
  labels:
    {{- include "drawio-agent.labels" . | nindent 4 }}
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: {{ .Values.gateway.name }}
  {{- if .Values.httproute.hostnames }}
  hostnames:
    {{- toYaml .Values.httproute.hostnames | nindent 4 }}
  {{- end }}
  rules:
    # 1. API routes
    - matches:
        - path:
            type: PathPrefix
            value: /api/v1
      backendRefs:
        - name: {{ include "drawio-agent.fullname" . }}-api
          port: 3000
    # 2. WebSocket chat route
    # Note: When Gateway TLS is enabled, client connections to /api/v1/ws must use WSS (wss://).
    # The Gateway terminates TLS on port 443 and forwards plain WebSocket frames to the API service.
    - matches:
        - path:
            type: PathPrefix
            value: /api/v1/ws
      backendRefs:
        - name: {{ include "drawio-agent.fullname" . }}-api
          port: 3000
    # 3. Default route for frontend editor
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "drawio-agent.fullname" . }}-frontend
          port: 8080
