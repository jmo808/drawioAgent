{
  "name": "drawio-agent-monorepo",
  "private": true,
  "description": "Monorepo for DrawIO Agent - Self-hosted draw.io editor extended with AI chat sidebar",
  "workspaces": [
    "packages/shared",
    "services/api",
    "frontend/sidebar"
  ],
  "scripts": {
    "build": "npm run build -w packages/shared -w services/api -w frontend/sidebar",
    "typecheck": "npm run build -w packages/shared && tsc -b packages/shared/tsconfig.json services/api/tsconfig.json frontend/sidebar/tsconfig.json",
    "test": "npm run build -w packages/shared && npm run test -w packages/shared -w services/api -w frontend/sidebar",
    "lint": "npm run lint -w frontend/sidebar"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "typescript": "^5.3.3"
  }
}
